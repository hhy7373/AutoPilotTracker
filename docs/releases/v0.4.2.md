# AutoPilotLog v0.4.2

## 修复

- 修复阿里云 ECS 静态资源目录权限错误导致的线上白屏。
- 固化前端部署后的目录和文件权限，确保 Nginx 可以读取 Vite 生成的 JS/CSS。

## 验证

- Nginx 配置测试通过。
- 首页返回 HTTP 200。
- 首页引用的 JS 返回 HTTP 200，Content-Type 为 `text/javascript`。
- 首页引用的 CSS 返回 HTTP 200，Content-Type 为 `text/css`。
- API `/api/health` 返回 HTTP 200。

## 已知限制

- 当前公网站点仍使用 HTTP，HTTPS 证书和强制跳转尚未配置。
