# AutoPilotLog v0.2.2 — 2026-08-28

## 一句话摘要

修复生产云端为空时仍展示本地演示统计的问题。

## 修复

- Supabase 已配置时，版本统计和公开行程严格使用云端返回结果。
- 云端当前没有真实行程时，页面显示空数据状态和 0，而不再回退到本地种子数据。
- 只有未配置 Supabase 的本地开发/演示模式才使用本地演示数据。

## 验证

- `npm install`：通过。
- `npm run build`：通过。
- 线上环境变量已确认配置在 Production and Preview。
